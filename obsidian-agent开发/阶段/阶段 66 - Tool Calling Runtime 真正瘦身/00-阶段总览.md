# 阶段 66 - Tool Calling Runtime 真正瘦身

阶段 66 的目标是把 Phase 65 已经拆开的 Tool Calling 运行时继续瘦身到一个清晰生产入口，而不是保留
“新旧两条 loop + flag 切换”的灰区。

当前状态：`closeout_sync_authorized`。

核心路径：

```text
user text -> ToolCallingAgentService -> RunCoordinator -> ToolExecutor -> registry adapters
uploaded image -> ToolCallingAgentService -> RunCoordinator -> analyze_user_image
```

生产工具库存保持四个：`hybrid_search_knowledge`、`search_tables`、`search_figures`、
`analyze_user_image`。

`agent_run_coordinator_enabled` 已 deleted；回退方式是 rollback through Git，不是在生产里继续藏一条旧链路。

验收边界：

- PostgreSQL/pgvector judge-backed A/B 质量包已通过：A/B 各 30 text + 4 image，query/judge failure
  均为 0，B overall `0.870343137254902` >= A overall `0.8264705882352942`。
- 用户已在 2026-07-16 授权阶段 66 本地 Git、GitHub PR/merge 与 Obsidian 同步。
- 这不等同于 Phase65 holdout/judge 总门禁通过，也不等同于完整高成本 latency release gate 通过。
- SQLite A/B 只作为 runtime smoke，不作为 final acceptance evidence。
