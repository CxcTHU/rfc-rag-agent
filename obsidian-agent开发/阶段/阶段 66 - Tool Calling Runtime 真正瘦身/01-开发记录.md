# 阶段 66 - 开发记录

- 建立 Phase66 runtime/contract snapshot 脚本与测试。
- 引入 typed runtime ports、tool contracts、tool registry、tool result cache。
- 抽出 hybrid/table/figure/user image adapters，`tools.py` 变成薄 facade。
- `ToolExecutor` 改成 registry-driven dispatch。
- 抽出 final prompt、pre-tool gates、tool calling composition、tool result merge。
- 删除生产 `agent_run_coordinator_enabled` 双路径 flag。
- 让文字请求与上传图片请求都进入同一个 `RunCoordinator` 路径。
- 修正 Agent 默认模型：未显式选择时默认 `deepseek-v4-flash`，显式 Pro 仍被尊重。
- 对 pure figure lookup 跳过不必要的 hybrid supplement，保留解释型图像问题的图像+文本补证。
- 建立固定常用 Agent 回归集：`data/evaluation/agent_regression_cases.csv`，30 text + 4 image。
- 修复拒答展示策略：用户可见答案说明项目范围、拒答原因和可改问方向，不裸露 `off_topic`。
- 最终门禁：`tool_calling_service.py <= 260 lines`、`ToolCallingAgentService.query <= 80 lines`、
  `run_coordinator.py <= 120 lines`。

验证摘要：

- backend: `1897 passed, 1 skipped`
- focused Phase65 runtime regression: `199 passed`
- frontend unit/lint/build: passed
- fault matrix: `output/phase66/final/fault-matrix.json`
- runtime recovery: `output/phase66/final/runtime-recovery.json`
- final structure: `output/phase66/final/runtime-structure.json`
- PG/pgvector judge-backed A/B：`output/phase66/evaluation_pg_judge_fixed/`，30 text + 4 image per lane，
  query/judge failure 为 0，merge `status=passed`
- refusal strategy focused regression：`8 passed`

人工结论：

- 用户已授权阶段 66 收口同步、GitHub PR 和检查通过后的 merge。
- 不夸大：本阶段只验收 Phase66 Tool Calling 瘦身与相关质量非回归；Phase65 holdout/judge 总门禁仍独立。
