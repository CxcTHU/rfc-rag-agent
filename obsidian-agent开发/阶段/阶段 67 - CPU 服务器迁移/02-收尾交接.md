# 阶段 67 - 收尾交接

## 当前结论

新 CPU 已运行完整 Phase 66 合并代码，Cloudflare 生产入口、Tailscale 维护入口、Agent 与 Judge 均通过
脱敏 smoke。Phase 66 的人工验收边界没有被扩大；Phase 67 仍等待用户独立观察与验收。

## 保留的回滚能力

- 旧 app image：`rfc-rag-agent:pre-phase66-79aec024`；
- 旧 CPU：`rfc-cpu-old`，app/db/redis 继续保留；
- 旧 Cloudflare connector：配置保留，但必须维持 inactive/disabled；
- 新机异常时先停止新 connector，再恢复旧 connector，禁止两端同时接入独立数据库。

## 剩余风险与后续

- Tailscale systemd 已持久化，但节点 key expiry 为 2027-01-14；需在管理台关闭 expiry 才能形成长期
  免维护 SSH；
- 新 CPU 尚无经验证的定时异机备份；
- 旧 CPU、迁移 dump 与 transfer 备份只能在用户明确验收 Phase 67 后清理；
- Phase 65 holdout/judge 总门禁和广义 latency release gate 仍是独立事项。

## Git 同步状态

- 本地收口提交：`6fe16606`；
- 远端分支：`codex/phase-67-cpu-migration`；
- GitHub PR：#45，已创建，等待最终 HEAD checks；checks 失败时不得强行 merge。

恢复入口见 [[03-文件地图与恢复顺序]]。
