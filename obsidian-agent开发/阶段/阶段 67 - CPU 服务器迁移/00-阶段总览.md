# 阶段 67 - CPU 服务器迁移

当前状态：`workflow_persistence_verified_sync_in_progress`。

阶段 67 将已经人工验收的 Phase 66 Agent 迁移到替代 CPU。稳定维护入口为 `rfc-cpu`（Tailscale），
部署目录为 `/home/ubuntu/rfc-rag-agent-stage44-smoke`，公网入口为 Cloudflare Tunnel，不依赖平台开放
HTTP 入站端口。

## 已完成

- `.env.prod`、PostgreSQL、Redis 逻辑状态与 durable `data/` 通过服务器间链路迁移，未进入本地或 Git；
- 新机 app/db/redis healthy，app 为 host network，DB/Redis 只绑定回环；
- Cloudflare 新 connector active/enabled，旧 connector inactive/disabled，四域名 health 200；
- Tailscale 与 provider forward active/enabled，`rfc-cpu-old` 保留旧机回滚；
- 识别并修正“宿主源码新、live image 旧”的遗漏；当前镜像为
  `rfc-rag-agent:phase66-1af07fc1` / `1296fcc926a0`；
- 真实 Agent 与 Judge 均返回 200，公网前端服务 `index-DDE0lgzL.js`。

## 验收边界

- Phase 66 已由用户人工验收并完成代码上线；
- Phase 67 的迁移观察期、旧 CPU 下线与备份清理尚未获得独立验收；
- Tailscale key 将于 2027-01-14 到期，异机定时备份尚未建立，均为后续运维风险。

## 2026-07-19 增补：真实步骤持久化

- 修复“实时显示完整步骤、重载后只剩最终两步”的一致性缺陷；
- 完整安全运行步骤写入独立 `runtime_workflow_steps`，不改变 `workflow_steps` / `tool_calls` 语义；
- 3-case Chromium E2E 覆盖 6 步 reload、4 步 Query cache 清空后重新登录、旧会话 2 步回退，自动验证均 PASS；
- 本结论只证明步骤持久化与恢复一致性，不扩大为 Phase 67 总体验收或 Agent 广义质量验收。

相关：[[01-开发记录]]、[[02-收尾交接]]、[[03-文件地图与恢复顺序]]。
