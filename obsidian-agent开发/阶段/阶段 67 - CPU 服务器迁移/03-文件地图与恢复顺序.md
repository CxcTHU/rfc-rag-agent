# 阶段 67 - 文件地图与恢复顺序

## 真相源与入口

- 项目规则：`AGENT.MD`；
- 代码：GitHub workflow-persistence merge commit `29711f0fb1aeb78b4483be58fecbbb9ad3436c14`；
- 当前 live image：`rfc-rag-agent:production` / `2e8957f449c2`；直接回滚：`rfc-rag-agent:pre-workflow-1296fcc9` / `1296fcc926a0`；
- 部署目录：`/home/ubuntu/rfc-rag-agent-stage44-smoke`；
- 新机维护：`rfc-cpu` / `rfc-cpu-new`；公网回退：`rfc-cpu-new-public`；
- 旧机回滚：`rfc-cpu-old`；
- Cloudflare unit：`cloudflared-rfc-rag-agent.service`；
- Nginx origin：`127.0.0.1:8044`；app：host network `18044`；
- PostgreSQL / Redis：`127.0.0.1:15432 / 16379`；
- 本地状态：`task_plan.md`、`findings.md`、`progress.md`、`handoff.md`。
- 后端持久化：`app/api/agent.py`、`app/schemas/agent.py`；
- 前端恢复：`frontend/src/features/chat/model.ts`、`frontend/src/features/trace/workflow.ts`；
- E2E 集合：`frontend/e2e/fixtures/workflow-persistence-cases.json`、`frontend/e2e/specs/workflow-persistence.spec.ts`、`frontend/e2e/mock-server.mjs`。

`.env.prod`、Cloudflare credential、数据库 dump、SSH key、私有日志和 provider raw response 不得进入
本地文档、Obsidian 或 Git。

## 恢复顺序

1. 阅读 `AGENT.MD` 与本阶段四类文件；
2. 通过 `rfc-cpu` 检查 app/db/redis healthy，确认 app network mode 为 host；
3. 直接在 live container 核对 image revision、三个 Phase 66 默认、等待态与当前 frontend asset；
4. 检查 Cloudflare/Tailscale/provider forward active/enabled，旧 connector inactive/disabled；
5. 检查四域名 `/health` 与公网 frontend asset；需要时用短期内存 token 做脱敏 Agent/Judge smoke；
6. 若新机异常，先停新 connector，再恢复旧机；禁止双 connector 对独立 PostgreSQL 写入；
7. 用户验收 Phase 67 后再决定旧 CPU、dump 和 transfer 备份清理；
8. 在 Tailscale 管理台关闭新节点 key expiry，并建立、演练异机备份。

## 增补恢复顺序

1. 运行后端 stream conversation persistence 回归，确认 metadata 与 GET messages 的 `runtime_workflow_steps` 一致；
2. 运行前端 unit 与 `workflow-persistence.spec.ts`，确认 6/4/2 三类恢复步骤保持一致；
3. 新 CPU 部署只能来自 GitHub 合并 commit，必须保留原 live image 回滚标签；
4. 部署后从 live container 核对新字段代码与 frontend asset，再做认证 Agent 查询和同一 conversation messages 回读；
5. Cloudflare 实际 unit `cloudflared-rfc-rag-agent.service` 与 Tailscale 必须继续 active/enabled，DB/Redis 继续只绑定回环；
6. 检查不得执行会展开环境值的 `docker compose config`；完成相关生产凭据轮换并留下不含值的轮换收据。
