# 阶段 67 - 开发记录

## 迁移与拓扑

- 应用代码真相源：GitHub merge commit `1af07fc145e32ed2cbf1a79d59f0877d802c408d`；
- 服务器本地状态真相源：旧 CPU 的 `.env.prod`、PostgreSQL、Redis 与 `data/`；
- Cloudflare Tunnel 将四个 hostname 转发到新机 Nginx `127.0.0.1:8044`；
- 日常 SSH 经 Tailscale，公网 SSH 仅作回退；未复制旧 `/var/lib/tailscale` 节点身份。

## 部署遗漏与补正

首次迁移因 Docker Hub 超时复用了旧 app image `79aec024f642`。该镜像创建于 Phase 66 之前；宿主机
源码虽有新标记，live container 仍缺 `agent_default_chat_model`、生产默认提升和新前端资产。

补正步骤：

1. 证明 Phase 66 最终 tree 与 GitHub merge tree 一致，且 `pyproject.toml` 无依赖变化；
2. rsync 385 个 merged runtime 文件，不覆盖 `.env.prod`、数据库、Redis 或 `data/`；
3. 以旧生产镜像为离线 dependency base，构建 `phase66-1af07fc1`，保留旧镜像回滚标签；
4. 在 18045 canary 验证 health、三个生产默认、等待态和 frontend asset；
5. 切换 production app，再执行真实 Agent、Judge、四域名和持久服务复核。

## 验证摘要

- live image：`1296fcc926a0`，OCI revision 为完整 `1af07fc1...`；
- short-loop / route-first / retrieval fan-out：全部 true；默认模型：`deepseek-v4-flash`；
- Agent：200、`tool_calling_agent`、3 citations/12 sources；Judge：200/completed；
- Cloudflare、Tailscale、provider forward：active/enabled；旧 connector：inactive/disabled；
- 四域名 health 200，公网 frontend asset：`index-DDE0lgzL.js`。

所有 smoke 只保存状态与计数；未保存 raw answer、reasoning、provider response、完整 chunk、token 或密钥。

## 新词

- 离线覆盖镜像：在依赖未变化时，以已验证镜像为 base，只覆盖新代码与构建资产的镜像。
- live image proof：直接在运行容器内核对镜像 ID、revision、配置与资产，不能用宿主源码替代。
