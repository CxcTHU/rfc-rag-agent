# 阶段 64 - 收尾交接

> 状态：收尾完成
> 开发记录：[[阶段 64 - 开发记录]]
> 阶段总览：[[阶段 64 - 主流 Agent 延迟优化]]

## 发布状态

- 最终功能提交：`271710df`（`Complete phase 64 mainstream-agent latency`）。
- 阶段 tag：`phase-64-complete`，指向最终功能提交。
- GitHub PR：[#37](https://github.com/CxcTHU/rfc-rag-agent/pull/37)，已合并至 `main`。
- 合并提交：`f8ae3c4e`；已验证 tag 是 `origin/main` 的祖先。

## 新 Session 恢复顺序

1. `AGENT.MD`：长期宪法、测试/安全/协作门禁。
2. `handoff.md`：现场快照；先确认分支、工作树和禁止重复的动作。
3. `README.md`、`docs/progress.md`、`docs/architecture.md`、`docs/data_sources.md`：
   产品、长期状态、当前架构和数据安全边界。
4. `task_plan.md`、`findings.md`、`progress.md`：当前计划、已验证事实和即时交接状态。
5. `docs/phase_reviews/phase-64.md`：已结束阶段的冻结结论；不得把进行中状态写回该文件。
6. `scripts/evaluate_phase64_latency_ab.py`、`scripts/judge_phase64_latency_ab.py`、
   `scripts/probe_phase64_final_model_floor.py` 与 `data/evaluation/phase64_*`：需要复现
   延迟或质量结论时的证据入口。

`docs/decisions/ADR-xxx.md` 只在出现不可逆设计决定时加入。本阶段没有新增 ADR：
Route-First、fan-out、rerank 路由与提示预算均由 feature flag 或部署配置控制，可回退。

## 交付物

- 阶段设计与执行计划：`docs/superpowers/specs/2026-07-13-phase-64-*.md`、
  `docs/superpowers/plans/2026-07-13-phase-64-*.md`。
- 冻结评审：`docs/phase_reviews/phase-64.md`。
- 现场快照：仓库根目录 `handoff.md`。
- 当前计划、事实与进度：根目录 `task_plan.md`、`findings.md`、`progress.md`。
- 可复跑安全证据：`data/evaluation/phase64_*` 与相应脚本。

## 最终验收

- 用户人工功能验收：PASS。
- 后端：`1479 passed, 1 skipped`。
- 前端：31 项单测、lint、生产构建通过。
- Stage 30：`91.52 / A / pass`。
- GitHub CI：Python deterministic tests、frontend lint/unit/build/e2e、PostgreSQL
  Alembic smoke、Docker build、secret scan 均通过。

## 未完成但已冻结的事项

- 原冷链路首 token P95 ≤15 秒没有通过或被完整验证；三对方向性 B Flash 样本 P95
  为 18.683 秒。
- 30×3 冻结 A/B、盲评和质量非劣统计留给下一阶段。
- 下一阶段优先建立端到端冷启动门禁，再评估持久化 lexical snapshot 与最终模型
  服务容量/lane；不得以关闭 rerank、固定 Dynamic-K 或伪造首 token 取代验证。
