# 开发记录

## 开工与盘点

- 完整读取规则、README、进展、架构、数据源、根计划/发现/进度/交接。
- 原工作区存在 Phase 67 遗留改动，故从最新 `origin/main` `0c0f042d` 建立隔离工作树与
  `codex/phase-68-code-cleanup`。
- 清洗前基线：后端 `1936 passed, 1 skipped`；前端 `8 files / 33 tests passed`。
- 只读引用图确认同步/流式入口均直接构造 `ToolCallingAgentService`。

## TDD 与实现

- 新增 Phase 68 单链路契约，首轮得到 6 个红灯：退休文件仍存在、历史 mode 被静默忽略、
  LangGraph 依赖仍在、工具顺序断言过严。
- 把当前链路仍使用的图片意图判断从 `react_actions.py` 迁入中性 `intent_router.py`。
- 删除 ReAct、LangGraph、旧 Agent memory/default executor 和专属测试/评测。
- `service.py` 只保留生产共用 `AgentQueryResult`，默认 mode 改为 `tool_calling_agent`。
- API request mode 收窄为唯一字面量；meta、capability、refusal explanation、chitchat、follow-up、
  同步/SSE 响应 mode 全部统一。
- 旧静态工作台不再为图片请求附加 `react_agent`；production smoke 移除历史回滚 case。
- 当前 Tool Calling Judge 从旧对比脚本解耦到 `tool_calling_eval_cases.py` 和
  `tool_calling_eval_support.py`。
- 旧 `routing.py` default/agentic 自动复杂度分流确认只被专属测试引用，已删除。
- Reranker A/B 内置 case 从已删除 Phase51 性能脚本切到当前中性 Tool Calling case；旧 CLI 参数仅作为别名。

## 本地验证

- 后端全量：`1736 passed, 1 warning`。
- 前端：Vitest `33 passed`，oxlint 通过，build 通过，Playwright `10 passed`。
- 聚焦默认链路/清洗包：`120 passed, 1 warning`。
- 定向 Ruff：本次新增/修改 Python 文件 `All checks passed`；全仓 Ruff 仍有既有历史问题，不作为本阶段新增阻断。
- `compileall` 通过，`git diff --check` 通过。

## 已知开发错误

- planning catch-up 默认 Python 路径不存在，改用当前 `python`。
- 首次 PowerShell `rg` 引号错误，拆分为单引号表达式。
- Obsidian Phase 66 目录名猜错，先列目录后按真实名称读取。
- 首次从旧 Stage 38 文件抽取 case block 未处理 CRLF 边界，未写入文件；第二次生成后补正末尾括号并通过 compileall。
- 首轮定向回归 4 个失败来自清洗后的预期合同差异，修正后 `102 passed`。
- Chainlit fixture 仍用旧 `mode=default`，改为 `tool_calling_agent` 后聚焦包通过。
- 全量后端首次 collection 暴露 3 个已删除旧脚本专属测试，删除后重跑。
- 全量后端 reranker Stage3 懒加载已删除 Phase51 脚本，改为中性 Tool Calling case 后通过。
- 首次 `git diff --check` 报 Windows build 产物与 EOF 空行，机械修正后通过。
