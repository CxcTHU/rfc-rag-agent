# 阶段 68 - 代码清洗

状态：`local_verification_passed_git_pr_pending`

目标是让代码树与当前生产事实一致：在线 Agent 只保留 `tool_calling_agent`，不再让退休的
default `AgentService`、ReAct、LangGraph 和旧 Agent memory 路径干扰维护判断。

```text
/agent/query or /agent/query/stream
-> ToolCallingAgentService
-> RunCoordinator
-> ToolExecutor
-> tool_registry / typed adapters
-> AgentToolbox
```

删除范围包括退休服务、两套 LangGraph 图、graph state/nodes/builder/checkpointer、旧
`memory_context`、专属测试/评测脚本以及 LangGraph Python 依赖。API 只允许省略 `mode` 或显式
使用 `tool_calling_agent`；其他历史 mode 返回 422。

`legacy_toolbox.py` 明确保留，因为当前 `AgentToolbox` 仍继承它，四个生产工具
`hybrid_search_knowledge`、`search_tables`、`search_figures`、`analyze_user_image`
的实现都在其中。当前会话摘要 memory 与 Redis caches/rate limiting 也不是退休路径。

回滚方式是回退 Phase 68 Git commit 并重新安装依赖/部署，不提供在线 mode 切换。

当前本地验证收据：最终后端全量 `1736 passed, 1 warning`；前端 unit `33 passed`、lint/build 通过、
Playwright `10 passed`；聚焦默认链路/清洗包 `120 passed, 1 warning`；定向 Ruff 本次 Python 文件
通过；`compileall` 与 `git diff --check` 通过；定向敏感扫描高风险命中为 0。尚待 commit、
push、PR、checks、merge。
