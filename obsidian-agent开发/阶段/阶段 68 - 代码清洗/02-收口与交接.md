# 收口与交接

提交前门禁：

- [x] 完整后端测试：`1736 passed, 1 warning`；
- [x] 当前默认链路定向测试：`120 passed, 1 warning`；
- [x] 前端 unit、lint、build；
- [x] Agent 工作台 Playwright E2E：`10 passed`；
- [x] Ruff/compileall、`git diff --check`；
- [x] 只扫描本次变更的敏感信息门禁：高风险命中 0；
- [ ] 显式暂存并复核 staged diff；
- [ ] commit、push、PR 到 main；
- [ ] GitHub checks 全绿后 merge。

必须保留：

- `app/services/agent/legacy_toolbox.py`；
- `app/services/conversation/session_memory.py`；
- Redis query/evidence/layered cache 与 rate limiting；
- `AgentQueryResult` 当前运行时数据合同；
- 四个生产工具及 typed adapters。

禁止带入提交：`.env`、密钥/token、provider raw response、raw answer、`reasoning_content`、
完整 chunk、私有日志、`output/`、截图和原工作区的无关 Phase 64/67 改动。

回滚：恢复 Phase 68 前 Git revision、恢复当时 `pyproject.toml` 依赖并正常重建部署。不要尝试用
request `mode` 或环境 flag 复活已删除实现。
