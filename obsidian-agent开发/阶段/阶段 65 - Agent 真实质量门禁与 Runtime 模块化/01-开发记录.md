# 阶段 65 开发记录

## 2026-07-14：规划与设计

- 阅读项目规则、Phase 64 交接和长期文档。
- 从主流 Agent / 对抗性原则评估后，确定 Phase 65 合并两条主线：
  1. 可信质量门禁；
  2. Runtime / Tool-calling 模块化。
- 写入 Superpowers 设计规格与 65A / 65B / 65C 三段实施计划。

## 2026-07-14：65A 可信门禁

- 完成 manifest、stale gate、模型库存、contract snapshot、paired execution preflight、Stage30 stale gate。
- 建立安全 A/B 与 blind judge receipt 结构；默认 fail-closed。
- 迁入主工作区后完成定向交叉回归和迁移复审。
- 明确费用不作为当前阶段阻断条件；但真实执行、cold-cache、endpoint identity、receipt 仍必须满足。

## 2026-07-14 至 2026-07-15：65B Runtime 模块化

- 拆分 `tool_calling_service.py` 大主循环，形成独立职责边界：
  `RunCoordinator`、`ToolExecutor`、`EvidenceStateMachine`、`FinalAnswerController`、
  `CheckpointRepository`、`PlanningPolicy`、`RuntimeEventBus`、`final_result_assembler.py`。
- `RunCoordinator` 默认接管普通 query；旧 service 降为兼容组合入口。
- checkpoint / resume、completed tool replay guard、pre-tool gate、final answer/refusal checkpoint、安全 SSE 事件和 bounded diagnostics 已接入。
- 用户人工验收结论：Runtime / Tool-calling 拆解开发内容 PASS。
- 验收边界：仅确认拆解开发内容，不代表 Phase 65 总体验收 PASS。

## 2026-07-15：65C 验收链路推进

- 建立 private holdout intake / overlap guard / evaluator gate / judge receipt / acceptance summary / closeout audit。
- 真实 holdout A/B 已完成 12 cases × 2 lanes，执行行完整且 cold-cache receipt 有效。
- blind judge receipt 未完整通过；acceptance 与 closeout 仍 fail-closed blocked。
- 当前 blockers：`holdout_gate_not_pass`、`human_acceptance_not_pass`。

## 已验证记录

- Runtime 相关 focused / wider regression 已在开发过程中多轮通过。
- 最新本轮收口前 targeted Phase65 测试：`106 passed`。
- 本地 8000 端口人工核验前服务已启动，`/health` 与 `/` 返回 200。
- `git diff --check` 仅 CRLF 提示，无 whitespace error。

## 安全边界

- 不提交 `.env`、密钥、token、provider raw response、raw answer、`raw_response`、
  `reasoning_content`、完整 chunk、私有日志或 `output/` 产物。
- 不把 smoke、partial holdout 或局部人工验收写成总体验收 PASS。
