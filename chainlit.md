# RFC-RAG-Agent

面向堆石混凝土资料库的引用式 RAG Agent。

你可以直接提问：

- 堆石混凝土的概念、施工工艺、温控、充填性能和工程案例。
- 要求检索某个主题的资料并给出引用来源。
- Agent 统一走 `tool_calling_agent`，界面会展示当前链路返回的检索、工具调用和生成步骤。

本界面复用项目已有 FastAPI 服务层、会话持久化、闲聊短路、流式生成、引用溯源和重排序检索能力。真实 API key 只允许通过本地 `.env` 或运行时环境变量提供，不会写入回答附件、日志、测试、文档或 Docker 镜像。
